#include <stdio.h>
#include <stdbool.h>

int main()
{
    // bool val1 = true;
    // bool val2 = false;
    // printf("%d\n", val1);
    // printf("%d", val2);
    // <
    // >
    // <=
    // >=
    // ==
    // !=
    // &&
    // ||
    // !

    bool value = !(10 >= 15) && (12 == 12);
    printf("%d", value);

    return 0;
}